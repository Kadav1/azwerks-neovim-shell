# azwerks Neovim Shell v0.1.0 — GitHub Release Draft

## Summary

A GitHub Release draft was created for **azwerks Neovim Shell v0.1.0**.

The release remains unpublished and uses the existing pushed `v0.1.0` tag.

## Release Repository

```text
Kadav1/azwerks-neovim-shell
```

## Release Title

```text
azwerks Neovim Shell v0.1.0
```

## Release Tag

```text
v0.1.0
```

The release uses the existing pushed `v0.1.0` tag.

No new tag was created.

The existing tag was not moved.

## Draft Status

```text
isDraft: true
isPrerelease: false
```

The release remains a draft and has not been published.

## Attached Asset

```text
azwerks-nvim-shell-v0.1.0-source.tar.gz
```

Source path:

```text
dist/azwerks-nvim-shell-v0.1.0-source.tar.gz
```

## Checksum

SHA-256:

```text
bd7db5be0430dcfbc3c0ecaed65e8ca99bf918f0bd6a94293f8814d55f200a55
```

## Release URL

```text
https://github.com/Kadav1/azwerks-neovim-shell/releases/tag/untagged-f00bd411bc3545ed26d0
```

GitHub reports the release `tagName` as:

```text
v0.1.0
```

The current draft URL uses GitHub’s temporary `untagged-*` draft-release URL
form. Treat the `tagName`, draft status, attached asset, and GitHub release
metadata as the authoritative release fields before publication.

The public release URL may change after the draft is published.

## What Was Uploaded

* Clean source archive only:

  * `azwerks-nvim-shell-v0.1.0-source.tar.gz`

## What Was Not Uploaded

* No deb package.
* No rpm package.
* No AppImage.
* No Flatpak.
* No extra package artifacts.

## What Was Not Published

* The GitHub Release was not published.
* The `v0.1.0` tag was not moved.
* No new tag was created.
* No force push was used.
* No package distribution track was started.

## Release Scope

This draft represents a **source-only, user-local v0.1.0 release candidate**.

It does not represent a packaged distribution release.

## Notes

This project remains a GTK/VTE wrapper around terminal Neovim.

It is not a true Neovim GUI frontend.

It does not use `nvim --embed`.

It does not implement the Neovim external UI protocol.
